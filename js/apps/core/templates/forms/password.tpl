<div class="form-error error"></div>

<fieldset class="narrow-fieldset">
  <fieldset class="fieldset-row" id="fbRegistrantMsg" style="display:none;">
    <div>
      <label> You registered with Facebook and therefore don't have an Equinox password yet. You may create one now.</label>
    </div>
  </fieldset>
  <fieldset class="fieldset-row">
        <div data-fields="currentPassword"></div>
    </fieldset>

    <fieldset class="fieldset-row">
        <div data-fields="newPassword"></div>
    </fieldset>

    <fieldset class="fieldset-row">
        <div data-fields="passwordConfirm"></div>
    </fieldset>

    <fieldset class="fieldset-row">
        <div><label>6-20 characters include at least one letter and number.</label></div>
    </fieldset>
</fieldset>