(function (global, App) {
    'use strict';

    /**
    * Component Init.
    */

    App.Components['search-menu-box'] = function () {
        //debug('[Search-Menu-Component]');
        /*var $searchBox = $el;
        if (!user) {
            $searchBox.addClass('hidden');
        } else {
            $searchBox.find('.logged-in-submenu').on('click', function (e) {
                e.preventDefault();
                $(this).toggleClass('active');
                $searchBox.find('.logged-in-expanded-submenu .submenu').toggle();
            });
        }*/
    };

} (window, window.App));