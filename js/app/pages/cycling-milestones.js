(function (App) {
    'use strict';

    /**
     * Milestones component
     */
    App.Components['cycling-milestones'] = function ($el) {
        console.log('MILESTONE INIT', $el);
    };

}(window.App));